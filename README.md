# student-exam-performance-data-modeling
This project decomposes a 100,000 unique rows and 44 columns table into a normalized relational schema, builds an Entity Relationship Diagram (ERD), and implements a SQL based ETL pipeline that cleans and loads the raw data into the new schema.
